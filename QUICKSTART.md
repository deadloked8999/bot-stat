# Быстрый старт

## За 5 минут запустите бота

### Шаг 1: Создайте бота в Telegram (2 минуты)

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "My Stat Bot")
4. Придумайте username для бота (должен заканчиваться на "bot", например: "my_stat_bot")
5. Скопируйте токен, который выдаст BotFather (выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Установите зависимости (1 минута)

Откройте командную строку в папке с ботом и выполните:

```bash
pip install -r requirements.txt
```

### Шаг 3: Настройте токен (1 минута)

**Windows PowerShell:**
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
```

**Windows CMD:**
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

**Или** откройте файл `config.py` и замените строку:
```python
BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
```
на:
```python
BOT_TOKEN = 'ваш_токен_здесь'
```

### Шаг 4: Запустите бота (1 минута)

**Windows:**
Дважды кликните на `start.bat`

**Или через командную строку:**
```bash
python bot.py
```

Вы должны увидеть:
```
🤖 Бот запущен!
Для остановки нажмите Ctrl+C
```

### Шаг 5: Проверьте работу

1. Найдите вашего бота в Telegram (по username)
2. Отправьте команду `/старт москвич`
3. Бот должен ответить с приветствием и списком команд

## Первый ввод данных

Попробуйте ввести тестовые данные:

1. Отправьте: `/старт москвич`
2. Отправьте: `нал`
3. Скопируйте и вставьте:
   ```
   Д1 Жанна 2200
   Р7 Иван 700
   К3 Антон 500
   ```
4. Отправьте: `готово`
5. Отправьте: `дата/записать`

Готово! Данные сохранены. Теперь можно запросить отчет:

```
прошу отчёт
```

## Команды для быстрого доступа

```
/старт москвич     - Начать работу с клубом Москвич
/старт анора       - Начать работу с клубом Анора
нал                - Ввести данные НАЛ
безнал             - Ввести данные БЕЗНАЛ
готово             - Завершить ввод
дата/записать      - Сохранить данные
прошу отчёт        - Получить отчёт
```

## Проблемы?

### Бот не запускается
- Проверьте, что Python установлен: `python --version` (должна быть версия 3.8+)
- Проверьте, что установлены зависимости: `pip list | grep telegram`

### Бот запускается, но не отвечает
- Проверьте правильность токена
- Убедитесь, что нет других процессов с этим ботом
- Перезапустите бота

### Другие проблемы
Смотрите полную документацию в `README.md`

