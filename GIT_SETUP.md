# 📦 Инструкция по созданию Git репозитория

## ✅ Локальный Git уже инициализирован!

Первый коммит создан:
- 34 файла
- 7777 строк кода
- Коммит: "Initial commit: Telegram bot for statistics tracking v2.0"

---

## 🌐 Создание репозитория на GitHub (5 минут)

### Шаг 1: Создайте репозиторий

1. Откройте https://github.com
2. Нажмите кнопку **"New"** или **"New repository"** (зелёная кнопка)
3. Заполните:
   - **Repository name:** `bot-stat` (или любое другое имя)
   - **Description:** "Telegram bot for statistics tracking"
   - **Private** - выберите если хотите приватный репозиторий
   - **НЕ СТАВЬТЕ галочки:** "Add README", "Add .gitignore" (уже есть)
4. Нажмите **"Create repository"**

### Шаг 2: Скопируйте URL

GitHub покажет URL, например:
```
https://github.com/YOUR_USERNAME/bot-stat.git
```

Скопируйте этот URL!

### Шаг 3: Выполните команды

Откройте PowerShell в папке проекта и выполните:

```bash
git remote add origin https://github.com/YOUR_USERNAME/bot-stat.git
git branch -M main
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш username в GitHub!**

### Шаг 4: Готово!

Ваш проект опубликован на GitHub! 🎉

---

## 🔐 ВАЖНО: Безопасность токена

### Перед push проверьте:

1. Файл `.gitignore` существует ✅
2. В `.gitignore` есть `*.db` ✅
3. Токен в `config.py` - замените на placeholder!

### Защита токена:

Откройте `config.py` и замените:
```python
BOT_TOKEN = '8529542965:AAG2hgRXjWSCBbWVGx57fknqSfZuTumE2bs'
```

на:
```python
BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
```

Затем:
```bash
git add config.py
git commit -m "Hide bot token"
git push
```

---

## 🚀 Альтернатива: GitLab

### GitLab (если не хотите GitHub):

1. Откройте https://gitlab.com
2. Нажмите "New project"
3. "Create blank project"
4. Заполните название
5. Скопируйте URL
6. Выполните команды выше (замените GitHub URL на GitLab URL)

---

## 📋 Быстрая команда для push

После создания репозитория на GitHub, выполните:

```bash
cd "C:\Users\denis\OneDrive\Desktop\bot stat"
git remote add origin https://github.com/YOUR_USERNAME/bot-stat.git
git branch -M main
git push -u origin main
```

**Не забудьте заменить `YOUR_USERNAME`!**

---

## ✅ Проверка

После push откройте:
```
https://github.com/YOUR_USERNAME/bot-stat
```

Вы должны увидеть все файлы проекта!

---

## 🆘 Если нужна помощь

### Ошибка: "Permission denied"
```bash
# Настройте Git credentials
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
```

### Ошибка: "Repository not found"
- Проверьте правильность URL
- Убедитесь что репозиторий создан

---

**ТЕКУЩИЙ СТАТУС:**
- ✅ Локальный Git инициализирован
- ✅ Первый коммит создан
- ⏳ Ожидание создания удалённого репозитория

