# Telegram Бот для учета статистики

Бот для учета и анализа данных по двум каналам (НАЛ и БЕЗНАЛ) для двух клубов.

## Возможности

- ✅ Блочный ввод данных (поддержка вставки из Excel)
- ✅ Поддержка двух клубов: Москвич и Анора
- ✅ Автоматический парсинг и валидация данных
- ✅ Генерация отчетов с расчетами и проверкой
- ✅ Редактирование и удаление записей
- ✅ Экспорт отчетов в XLSX
- ✅ Журнал всех изменений

## Установка

### 1. Установите Python 3.8 или выше

Скачайте с [python.org](https://www.python.org/downloads/)

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте бота в Telegram

1. Найдите бота [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен, который выдаст BotFather

### 4. Настройте токен

**Способ 1 (рекомендуется):** Через переменную окружения

Windows PowerShell:
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
```

Windows CMD:
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

Linux/Mac:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
```

**Способ 2:** Измените файл `config.py`

Откройте `config.py` и замените:
```python
BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
```

на:
```python
BOT_TOKEN = 'ваш_токен_здесь'
```

### 5. Запустите бота

```bash
python bot.py
```

## Использование

### Начало работы

1. Выберите клуб:
   - `/старт москвич` — выбрать клуб Москвич
   - `/старт анора` — выбрать клуб Анора

### Ввод данных

1. Начните ввод канала:
   - `нал` — для ввода данных по НАЛ
   - `безнал` — для ввода данных по БЕЗНАЛ

2. Вставьте список в формате:
   ```
   Д1 Жанна 2200
   Р7 Иван 700
   К3 Антон 500
   ```
   
   Формат: `<код> <имя> <сумма>`
   
   - Код: буквенно-цифровой (Д1, R7, K3)
   - Имя: может состоять из нескольких слов
   - Сумма: число (можно с запятой или точкой: 1200,50 или 1200.50)

3. Завершите ввод:
   - `готово` — обработать введенные данные

4. Сохраните данные:
   - `дата/записать` — сохранить на текущую дату
   - `дата/записать 2025-11-03` — сохранить на конкретную дату

### Просмотр отчетов

- `прошу отчёт` — отчет за текущую неделю по активному клубу
- `прошу отчёт москвич` — отчет по клубу Москвич
- `прошу отчёт неделя 2025-11-03..2025-11-09 москвич` — за конкретный период

Отчет включает:
- Имя и код сотрудника
- Сумма по НАЛ
- Сумма по БЕЗНАЛ
- 10% от безнала
- Итог (нал + безнал − 10%)
- Автоматическая проверка совпадения сумм

### Редактирование данных

- `список 2025-11-03` — показать все записи за дату
- `исправить Д1 нал 2500 2025-11-03` — изменить сумму
- `удалить Д1 безнал 2025-11-03` — удалить запись

### Экспорт

- `экспорт неделя москвич` — экспортировать отчет в XLSX
- `экспорт неделя анора` — экспортировать для клуба Анора

## Примеры использования

### Пример 1: Ввод данных за день

```
Пользователь: /старт москвич
Бот: ✅ Выбран клуб: Москвич
     📅 Дата: 2025-11-03

Пользователь: нал
Бот: 📝 Режим ввода: НАЛ...

Пользователь:
Д1 Жанна 2200
Р7 Иван 700
К3 Антон 500

Пользователь: готово
Бот: ✅ Принято 3 строк
     Канал: НАЛ
     Клуб: Москвич
     ...
     💰 Итого по блоку: 3400.00

Пользователь: безнал
Бот: 📝 Режим ввода: БЕЗНАЛ...

Пользователь:
Д1 Жанна 15000
Р7 Иван 8000

Пользователь: готово
Бот: ✅ Принято 2 строк...

Пользователь: дата/записать
Бот: ✅ Сохранено: клуб Москвич, дата 2025-11-03
```

### Пример 2: Получение отчета

```
Пользователь: прошу отчёт
Бот: 📊 ОТЧЕТ
     Клуб: Москвич
     Период: 2025-11-03 .. 2025-11-09
     
     [таблица с данными]
     
     ✅ Сверка столбцов: совпадает
```

### Пример 3: Редактирование

```
Пользователь: список 2025-11-03
Бот: [список всех записей за дату]

Пользователь: исправить Д1 нал 2500 2025-11-03
Бот: ✅ Готово: Д1 нал 2200 → 2500 (2025-11-03)
```

## Структура проекта

```
bot-stat/
├── bot.py              # Главный файл бота
├── config.py           # Конфигурация
├── database.py         # Работа с базой данных
├── parser.py           # Парсинг входных данных
├── reports.py          # Генерация отчетов
├── utils.py            # Вспомогательные функции
├── requirements.txt    # Зависимости
├── README.md          # Документация
└── bot_data.db        # База данных SQLite (создается автоматически)
```

## Технические детали

### База данных

Используется SQLite с двумя таблицами:
- `operations` — основные записи (код, дата, клуб, канал, сумма)
- `edit_log` — журнал всех изменений

### Временная зона

По умолчанию используется `Europe/Riga`. Можно изменить в `config.py`.

### Нормализация кодов

Коды автоматически приводятся к единому виду:
- Кириллица → латиница (Д→D, Р→R, К→K)
- Верхний регистр

### Расчет отчетов

Формулы:
- `10% от безнала = round(Безнал * 0.10, 2)`
- `Итог = round(Нал + (Безнал - 10%), 2)`

Автоматическая проверка совпадения итогов по строкам и по пересчету.

## Устранение проблем

### Бот не запускается

1. Проверьте, что установлены все зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Проверьте токен бота в `config.py` или переменной окружения

3. Проверьте версию Python (должна быть 3.8+):
   ```bash
   python --version
   ```

### Бот не отвечает

1. Убедитесь, что бот запущен (в консоли должно быть "🤖 Бот запущен!")
2. Проверьте, что выбран клуб командой `/старт`
3. Проверьте правильность команд (они не чувствительны к регистру)

### Ошибки парсинга

- Проверьте формат: `код имя сумма`
- Убирайте пробелы внутри чисел: пишите `12000`, а не `12 000`
- Используйте точку или запятую для десятичных: `1200.50` или `1200,50`

## Безопасность

⚠️ **Важно:**
- Не публикуйте токен бота
- Не загружайте файл `config.py` с токеном в публичные репозитории
- Используйте переменные окружения для хранения токена

## Лицензия

Проект создан для внутреннего использования.

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение проблем"
2. Проверьте логи в консоли
3. Проверьте файл базы данных `bot_data.db`

