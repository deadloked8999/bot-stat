# Инструкция: Загрузка данных из Excel файла

## Описание
Альтернативный способ загрузки данных о сотрудниках и зарплатах через Excel файл вместо ручного блочного ввода.

## Как использовать

### 1. Запуск команды
Нажмите кнопку **📎 ЗАГРУЗИТЬ ФАЙЛ** в главном меню бота

### 2. Выбор клуба
Выберите клуб:
- 🏢 Москвич
- 🏢 Анора

### 3. Указание даты
Введите дату для данных в формате:
- `3,11` (03.11.2025)
- `30,10` (30.10.2025)

### 4. Загрузка файла
Отправьте Excel файл (.xlsx или .xls) в чат

### 5. Предпросмотр данных
Бот покажет:
- Список всех извлечённых данных (НАЛ и БЕЗНАЛ)
- Количество записей
- Итоговые суммы
- Возможные ошибки парсинга

### 6. Подтверждение
Введите:
- **ЗАПИСАТЬ** - сохранить данные в базу
- **ОТМЕНА** - отменить загрузку

## Формат Excel файла

### Требования к файлу:
1. Должен содержать блок **"Примечания"**
2. В блоке две колонки:
   - **Левая колонка** (Долг безнал) - данные для безналичных выплат
   - **Правая колонка** (Долг нал) - данные для наличных выплат

### Пример структуры:
```
Примечания
Долг безнал          | Долг нал
---------------------+---------------------
Д17 Юля 12000        | СБ5 Петя 8000
А3 Маша 5000         | УБОРЩИЦА 6000
Д4 Дима-3000         | К2 Света-4500
ИТОГО: 20000         | ИТОГО: 19100
```

### Поддерживаемые форматы записей:
- `Д17 Юля 12000`
- `Д7 Юля-1000`
- `Юля Д17 1000`
- `СБ Дмитрий 4000`
- `Уборщица-2000`
- `40,000` или `40.000` → 40000 ✅

## Особенности

### Автоматическая обработка:
- ✅ Извлечение данных из блока "Примечания"
- ✅ Парсинг кодов, имён и сумм
- ✅ Разделение на НАЛ и БЕЗНАЛ
- ✅ Удаление разделителей тысяч (точки/запятые)
- ✅ Очистка дублей из Excel (если скопировано несколько колонок)
- ✅ Предпросмотр перед сохранением

### Обработка ошибок:
- Показываются первые 3 ошибки парсинга
- Можно отменить загрузку на любом этапе (ОТМЕНА)
- Валидация формата файла (только .xlsx, .xls)
- Проверка наличия блока "Примечания"

## Сравнение с блочным вводом

| Характеристика | Блочный ввод | Загрузка файла |
|----------------|--------------|----------------|
| Скорость | Быстро для малого объёма | Быстро для большого объёма |
| Удобство | Копировать → Вставить | Один файл |
| Предпросмотр | ✅ Да | ✅ Да |
| НАЛ и БЕЗНАЛ | Раздельно | Одновременно |
| Источник данных | Любой текст | Excel файл |

## Что происходит после сохранения

После подтверждения (ЗАПИСАТЬ):
1. Данные сохраняются в базу (таблица `operations`)
2. Журналируются все изменения (таблица `edit_log`)
3. Можно работать с данными как обычно:
   - Смотреть отчёты (ОТЧЁТ)
   - Редактировать (ИСПРАВИТЬ)
   - Удалять (УДАЛИТЬ)
   - Экспортировать (ЭКСПОРТ)

## Технические детали

### Парсер Excel (`excel_parser.py`):
- Ищет заголовок "Примечания" (нечувствительно к регистру)
- Извлекает две колонки данных
- Останавливается при словах: "доход", "расход", "прибыль"
- Игнорирует строку "ИТОГО"

### Интеграция с DataParser:
- Каждая строка обрабатывается через `DataParser.parse_line()`
- Используется та же логика что и при блочном вводе
- Поддерживаются все форматы данных

### База данных:
- Данные сохраняются в таблицу `operations`
- Структура: `club`, `date`, `code`, `name`, `channel`, `amount`
- `aggregate=True` - суммы складываются при дубликатах

