# Как настроить токен бота

## Шаг 1: Получите токен у BotFather

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/botfather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Придумайте имя для бота (например: "My Stat Bot")
   - Придумайте username (должен заканчиваться на "bot", например: "my_stat_bot")
5. **Скопируйте токен** (выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## Шаг 2: Настройте токен (выберите один способ)

### Способ 1: Через переменную окружения (рекомендуется) ✅

#### Windows PowerShell
```powershell
$env:TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
python bot.py
```

Для постоянной установки:
```powershell
[System.Environment]::SetEnvironmentVariable('TELEGRAM_BOT_TOKEN', 'ваш_токен_здесь', 'User')
```

#### Windows CMD
```cmd
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
python bot.py
```

#### Linux / Mac
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
python bot.py
```

Для постоянной установки добавьте в `~/.bashrc` или `~/.zshrc`:
```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_здесь"
```

---

### Способ 2: Через файл config.py

1. Откройте файл `config.py` в текстовом редакторе
2. Найдите строку:
   ```python
   BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
   ```
3. Замените на:
   ```python
   BOT_TOKEN = 'ваш_токен_здесь'
   ```
4. Сохраните файл

**⚠️ Важно:** Не публикуйте файл `config.py` с токеном!

---

## Проверка настройки

Запустите бота:
```bash
python bot.py
```

### Правильно ✅
```
🤖 Бот запущен!
Для остановки нажмите Ctrl+C
```

### Ошибка ❌
```
❌ Ошибка: не установлен токен бота!
Установите переменную окружения TELEGRAM_BOT_TOKEN
или измените значение в config.py
```

---

## Безопасность токена

### ✅ Правильно:
- Хранить токен в переменной окружения
- Использовать `.gitignore` для `config.py` с токеном
- Не показывать токен другим людям

### ❌ Неправильно:
- Публиковать токен в GitHub/GitLab
- Отправлять токен в чатах
- Хранить токен в открытом виде

---

## Сброс токена (если он утёк)

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "API Token"
5. Нажмите "Revoke current token"
6. Получите новый токен
7. Обновите токен в настройках (см. Шаг 2)

---

## Дополнительные настройки BotFather

### Добавить описание бота
```
/mybots → выбрать бота → Edit Bot → Description
```

### Добавить команды (автодополнение в Telegram)
```
/mybots → выбрать бота → Edit Bot → Edit Commands
```

Вставьте:
```
старт - Начать работу с клубом
```

### Добавить изображение профиля
```
/mybots → выбрать бота → Edit Bot → Edit Botpic
```

---

## Что дальше?

После настройки токена:

1. ✅ Запустите бота: `python bot.py` или `start.bat`
2. ✅ Найдите бота в Telegram по username
3. ✅ Отправьте команду: `/старт москвич`
4. ✅ Следуйте инструкциям в `QUICKSTART.md`

---

## Пример .bat файла для Windows (с токеном)

Создайте файл `start_with_token.bat`:
```batch
@echo off
set TELEGRAM_BOT_TOKEN=ваш_токен_здесь
python bot.py
pause
```

Теперь можно запускать бота двойным кликом на этот файл!

**⚠️ Не делитесь этим файлом с другими!**

---

## FAQ

**Q: Где найти токен, если я его потерял?**  
A: Откройте [@BotFather](https://t.me/botfather), отправьте `/mybots`, выберите бота, нажмите "API Token"

**Q: Можно ли использовать один токен на двух компьютерах?**  
A: Нет! Бот может работать только в одном месте одновременно. Второй экземпляр вытеснит первый.

**Q: Нужно ли платить за токен?**  
A: Нет, создание бота и токен бесплатны.

**Q: Можно ли изменить username бота?**  
A: Нет, username нельзя изменить. Можно только создать нового бота.

**Q: Что делать, если токен не работает?**  
A: Проверьте:
1. Токен скопирован полностью (без пробелов)
2. Нет лишних символов
3. Бот не запущен где-то ещё

---

## Помощь

Если возникли проблемы:
1. Проверьте этот файл ещё раз
2. Смотрите раздел "Устранение проблем" в `README.md`
3. Проверьте файл `QUICKSTART.md`

